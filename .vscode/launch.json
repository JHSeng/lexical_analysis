{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "launch backend service",
            "program": "${workspaceFolder}/service/app.js"
        }
    ]
}